import 'package:flutter/material.dart';

class OnBoardingScreen extends StatelessWidget {
  const OnBoardingScreen({super.key});

  // Константы для цветов
  static const Color backgroundColor = Color(0xFF151515);
  static const Color continueBtnColor = Color(0xFF12B956);
  static const Color textColor = Color(0xFFF2F2F3);

  // Константы для текста
  static const String title = 'Тысяча курсов \nв одном месте';
  static const String favorite = 'Избранное';
  static const String alerts = 'Отклики';
  static const String message = 'Сообщения';
  static const String person = 'Профиль';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
